// Placeholder — replaced at deploy time by yapepay-services CD pipeline.
export const handler = async (event) => {
  console.log('Placeholder notification handler. Deploy from yapepay-services.');
  return { batchItemFailures: [] };
};
